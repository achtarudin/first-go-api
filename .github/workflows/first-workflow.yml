name: First Workflow

on: [workflow_dispatch, push]

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello, World!"
      - name: Print Good bye
        run: echo "Good bye, World!"