name: First Go API

on:
  workflow_dispatch:
  push:
    branches: [fix/workflow]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

