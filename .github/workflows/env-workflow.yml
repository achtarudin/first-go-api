name: First Go API

on:
  workflow_dispatch:
  push:
    branches: [fix/workflow]


jobs:
  test:
    environment: testing

    runs-on: ubuntu-latest

    env: 
       DEBUG: ${{ vars.DEBUG }}



    steps:
      - name: Show environment variables
        run: echo "Debug is ${{env.DEBUG}} "
